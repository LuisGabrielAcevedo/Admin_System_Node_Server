const { io } = require('../app');

//El servidor avisa cuando un usuario se conecta por socket
io.on('connection', (adminSystemServer) => {
    // adminSystemServer.on('connectUser', (data) => {
        
    // });
    // adminSystemServer.on('disconnectUser', (data) => {
        
    // });
    // adminSystemServer.on('usersOnlineRequest', (data) => {
    
    // });
});